# Обои нарисованы, а не найдены

`tex/wallfish_color.jpg` (детская) и `tex/hallpat_color.jpg` (коридор) сделаны
скриптом, а не скачаны. Скрипт лежит ниже целиком: без него текстуры не
пересоздать, а менять их придётся.

## Почему не нашли готовые

Искали, и вот чем кончилось:

- **ambientCG, наборы Wallpaper001A и 002A** — ровно белые, без рисунка. То
  есть в точности то, на что жаловались: «просто белые слегка шероховатые
  стены».
- **Poly Haven, decrepit_wallpaper** — бурый облезлый бетон. Комната должна
  читаться ОБЫЧНОЙ детской: страшно становится потом, а не с порога.
- **Poly Haven, Fabric Pattern 05** (клетчатая ткань) — поставили и получили
  приговор играющего: «как будто плитка общественного туалета, плюс рябит».
  Мелкий регулярный узор во всю стену глаз не выдерживает.

Вывод, который стоит запомнить: **обои — это плоский печатный рисунок**, и
рисовать их честнее, чем натягивать на стену фотографию ткани. Фотография
несёт своё освещение, свой ворс и свой масштаб, и всё это спорит со сценой.

## Что нарисовано

**Детская.** Синий фон с еле заметной вертикальной полосой и зерном, бледные
рыбы в раппорте СО СДВИГОМ через ряд (иначе рисунок выстраивается в решётку и
начинает рябить), направление чередуется, рядом пузырьки. Масштаб в игре 0.55 —
повтор в 1.8 м, рыба сантиметров двенадцать.

**Коридор.** Почти чёрный фон, ромбовидная решётка и ромбик в каждом узле.
Контраст нарочно низкий: узор должен угадываться, а не разглядываться. Важно:
тёмные стены требуют БОЛЬШЕ света, а не меньше, — при слабом фонаре узора не
видно вовсе, и стена читается пустой.

## Скрипт

Положить в проект как `_рис.gd`, рядом сцену с этим скриптом на корневом узле,
запустить `Godot --headless --path . _рис.tscn`. PNG появятся в `user://`
(на маке: `~/Library/Application Support/Godot/app_userdata/dotsofteror/`),
дальше перевести в jpg и положить в `tex/`.

```gdscript
extends Node
const N := 512

func _put(img: Image, x: int, y: int, c: Color, a: float = 1.0) -> void:
	var xx: int = ((x % N) + N) % N
	var yy: int = ((y % N) + N) % N
	img.set_pixel(xx, yy, img.get_pixel(xx, yy).lerp(c, clampf(a, 0.0, 1.0)))

func _fish(img: Image, cx: int, cy: int, s: float, flip: float, col: Color) -> void:
	var a: float = 17.0 * s
	var b: float = 7.5 * s
	var r: int = int(a * 2.2)
	for dx in range(-r, r + 1):
		for dy in range(-r, r + 1):
			var u: float = float(dx) * flip
			var v: float = float(dy)
			var inside := false
			if (u * u) / (a * a) + (v * v) / (b * b) <= 1.0:
				inside = true
			var t0: float = -a * 0.85
			var t1: float = -a * 1.75
			if u <= t0 and u >= t1:
				var k: float = (t0 - u) / (t0 - t1)
				if absf(v) <= b * (0.18 + k * 0.95):
					inside = true
			if u > -a * 0.3 and u < a * 0.45 and v < 0.0:
				var kk: float = 1.0 - absf(u / (a * 0.45))
				if -v <= b * (1.0 + kk * 0.55) and -v >= b * 0.55:
					inside = true
			if inside:
				_put(img, cx + dx, cy + dy, col, 0.95)
	var ex: int = cx + int(a * 0.55 * flip)
	for dx in range(-2, 3):
		for dy in range(-2, 3):
			if dx * dx + dy * dy <= 4:
				_put(img, ex + dx, cy - int(b * 0.25) + dy, Color(0.05, 0.10, 0.20), 0.9)

func _ready() -> void:
	var rng := RandomNumberGenerator.new()
	rng.seed = 20260915
	var fish := Image.create(N, N, false, Image.FORMAT_RGB8)
	for y in N:
		for x in N:
			var stripe: float = 0.035 * sin(float(x) * 0.049)
			var grain: float = (rng.randf() - 0.5) * 0.028
			fish.set_pixel(x, y, Color(0.20 + stripe + grain,
				0.34 + stripe * 0.8 + grain, 0.52 + stripe + grain))
	var pale := Color(0.78, 0.88, 0.94)
	for r in 4:
		for c in 4:
			var offx: int = 0 if r % 2 == 0 else int(float(N) / 4.0 * 0.5)
			var cx: int = int(float(N) / 4.0 * (float(c) + 0.5)) + offx
			var cy: int = int(float(N) / 4.0 * (float(r) + 0.5))
			var flip: float = 1.0 if (r + c) % 2 == 0 else -1.0
			_fish(fish, cx, cy, rng.randf_range(0.88, 1.12), flip, pale)
			for i in 3:
				var bx: int = cx + int(rng.randf_range(20.0, 44.0) * flip)
				var by: int = cy - int(rng.randf_range(10.0, 30.0))
				var br: int = rng.randi_range(2, 4)
				for dx in range(-br, br + 1):
					for dy in range(-br, br + 1):
						var d: float = sqrt(float(dx * dx + dy * dy))
						if d <= float(br) and d >= float(br) - 1.4:
							_put(fish, bx + dx, by + dy, pale, 0.5)
	fish.save_png("user://wallfish.png")

	var hall := Image.create(N, N, false, Image.FORMAT_RGB8)
	for y in N:
		for x in N:
			var g: float = (rng.randf() - 0.5) * 0.016
			hall.set_pixel(x, y, Color(0.055 + g, 0.052 + g, 0.058 + g))
	var step: int = N / 4
	for y in N:
		for x in N:
			if (x + y) % step < 2 or (x - y + N * 4) % step < 2:
				_put(hall, x, y, Color(0.15, 0.145, 0.16), 0.75)
	for r in 8:
		for c in 8:
			var nx: int = c * (N / 8) + (N / 16)
			var ny: int = r * (N / 8) + (N / 16)
			for dx in range(-7, 8):
				for dy in range(-7, 8):
					if absi(dx) + absi(dy) <= 7 and absi(dx) + absi(dy) >= 4:
						_put(hall, nx + dx, ny + dy, Color(0.20, 0.19, 0.205), 0.8)
	hall.save_png("user://hallpat.png")
	get_tree().quit()
```
